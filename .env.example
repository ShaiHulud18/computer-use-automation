# Required only for `discover` (the LLM-driven discovery run).
# Replay is deterministic and needs no key.
# Copy this file to .env (gitignored) and fill in your real key — never commit it.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the discovery model.
# MODEL=claude-sonnet-4-5
